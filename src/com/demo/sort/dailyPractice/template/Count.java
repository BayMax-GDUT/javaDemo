package com.demo.sort.dailyPractice.template;

public class Count {

    /**
     * 平均 O(n + k)
     * 最坏 O(n + k)
     * 最好 O(n + k)
     * 空间 O(k)
     * k是数据范围（max - min + 1）
     * 稳定
     */
    public static int[] sort (int[] array) {

        return array;
    }
}
